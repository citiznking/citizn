# functions

Supabase Edge Functions: all writes (reports, election check-ins/results/incidents, media signing) and the moderation API. Server-side geofence validation is the trust anchor — see spec.
